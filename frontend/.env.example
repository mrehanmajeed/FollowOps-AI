# Backend API base URL.
VITE_API_URL=http://localhost:8000/api/v1

# Optional: pre-fill the operator token during local development.
# Leave empty in any shared environment; the operator enters it in the UI and
# it is stored only in that browser's localStorage.
VITE_OPERATOR_API_KEY=
